package streams
import streams.GameDef

object Test {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(67); val res$0 = 
 new Pos(1,2);System.out.println("""res0: <error> = """ + $show(res$0))}
}